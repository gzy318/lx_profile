{
  "name": "lx-profile",
  "version": "33.0.0",
  "description": "An ultra-fast, zero-dependency personal profile site optimized for China mainland access.",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "build": "echo 'No build step required for pure functions'"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
